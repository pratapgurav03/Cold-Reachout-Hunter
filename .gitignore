# API keys — never commit this
.env
gmail_token.json
gmail_credentials.json
sender_config.json

# Python
__pycache__/
*.pyc
*.pyo
.Python
*.egg-info/

# Local output files
drafts/
hunt_*.json

# User data — contains personal contact info, don't commit
outreach_log.csv
seen_people.json

# Database (SQLite local dev — use PostgreSQL on Render)
*.db
*.sqlite3

# User-uploaded resumes (personal files, also ephemeral on Render)
resumes/

# macOS
.DS_Store

# Replit local cache
.replit.local
